export class Water {
    constructor(x,y,level=99999) {
        this.x =x;
        this.y = y;
        this.level = level
    }
    use (nanoai) {
        //check physical items of nanoai
        //if water bucket, fill water bucket
        //if fishing pole, start fishing activity
        //(if two fishing poles, duel wield fishing)
    }
}